<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Курси валют на сьогодні</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Курси валют на сьогодні</h1>

  <table border="1">
    <tr>
      <th>Валюта</th>
      <th>Купівля</th>
      <th>Продаж</th>
    </tr>
    <% rates.forEach(rate => { 
         const currency = currencies.find(c => c.id === rate.currencyId);
    %>
      <tr>
        <td><%= currency ? currency.name : 'Unknown' %> (<%= currency ? currency.code : '' %>)</td>
        <td><%= rate.buy %></td>
        <td><%= rate.sell %></td>
      </tr>
    <% }) %>
  </table>

  <hr>

  <h2>Перегляд історії курсу валюти</h2>
  <form action="/currency" method="get">
    <label for="currencyId">Оберіть валюту:</label>
    <select name="currencyId" id="currencyId" required>
      <% currencies.forEach(currency => { %>
        <option value="<%= currency.id %>"><%= currency.name %> (<%= currency.code %>)</option>
      <% }) %>
    </select>
    <br><br>

    <label for="startDate">Початкова дата:</label>
    <input type="date" id="startDate" name="startDate" required>
    <br>
    <label for="endDate">Кінцева дата:</label>
    <input type="date" id="endDate" name="endDate" required>

    <br><br>
    <button type="submit">Переглянути історію</button>
  </form>

  <hr>

  <a href="/admin">Перейти в адмін-панель</a>

</body>
</html>
